meta {
  name: Message Broker
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/sample/broker
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "message":"hello world"
  }
}
